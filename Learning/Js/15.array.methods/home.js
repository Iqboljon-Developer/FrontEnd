const royhat1 = [1, 11, 2, 3]
const royhat2 = [4, 5, 6]

const result = royhat1.concat(royhat2)
// console.log(result)

// const findValue = (number) => number === 2;
// const foundValue = result.find(findValue)

// const filter = (number) => number > 5;
// const foundValue = result.filter(number => number > 5)

// const mapfe = (number) 
// const foundValue = result.map(number => "Number: " + number)

const sortValues = (number1, number2) => number1 - number2;
result.sort(sortValues)
console.log(result)